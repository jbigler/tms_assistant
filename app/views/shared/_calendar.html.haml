#calendar_back.calendar_update= link_to t('calendar.previous'), calendar_back_path, :remote => true
#calendar_forward.calendar_update= link_to t('calendar.next'), calendar_forward_path, :remote => true
%br/
!= calendar( :year => @calendar_date.year, :month => @calendar_date.month, :first_day_of_week => 1, :abbrev => true ) do |d|
  - render_calendar_cell( d )
!= calendar( :year => @calendar_date.next_month.year , :month => @calendar_date.next_month.month, :first_day_of_week => 1, :abbrev => true ) do |d|
  - render_calendar_cell( d )
